#include <stdio.h>
int main()
{
    int n;
    printf("enter the  number :");
    scanf("%d",&n);

    if(n%4==0 && n%400==0)
{
    
     printf("the %d is leap year \n",n);

}
else
 printf("the %d is not leap year \n",n);

    return 0;
}
